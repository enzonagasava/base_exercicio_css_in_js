import { FormularioBackground, HeroTitle } from './Hero.ts'
import { Container } from '../../styles.ts'

const Hero = () => (
  <FormularioBackground>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </FormularioBackground>
)

export default Hero
